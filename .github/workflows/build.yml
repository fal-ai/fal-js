name: Build

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "24"
          cache: "npm"
      - name: "Set npm version"
        run: npm install -g npm@11.15.0
      - name: Install dependencies
        run: npm ci
      - name: Format check
        run: npx nx format:check --base=remotes/origin/${{ github.base_ref }}
      - name: Lint
        run: npx nx affected --target=lint --base=remotes/origin/${{ github.base_ref }}
      - name: Test
        run: npx nx affected --target=test --base=remotes/origin/${{ github.base_ref }}
      - name: Check Agent types and examples
        run: |
          npm run typecheck:agent
          node libs/client/examples/agent/recipes/run.mjs
          node libs/client/examples/agent/simple/run.mjs all
      - name: Build
        run: npx nx affected --target=build --prod --base=remotes/origin/${{ github.base_ref }}
      - name: Verify packed client on Node 18 and 22
        run: |
          npx nx build client --prod
          bash .github/scripts/verify-client-package.sh
